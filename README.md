# findethedox
use the Database of allmydox

take a database that is generated by allmydox and create a gui with the fowwowiing features
0. implemented in python
1. the user can input a word
2. the word and the nouns, names and verbs occuring in the same sentence should be visualized in the 3 wordlclouds (see 3.)
3. the words are represented by three wordclouds 1. names 2. nouns 3. verbs
4. the metric between the words is the frequency of each word and the frequency of being together in a sentence of a paragraph. Sentence counts 30% higher than paragraph.
5. if possible show the 3 word clouds even if the user did not giv an input
